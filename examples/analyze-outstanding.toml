schema_version = 1
operation = "analyze"

[analysis]
inputs = ["capture.nlx"]
mode = "leaks"
format = "json"
output = "outstanding.json"
from = "0s"
to = "900ms"

[filters]
min_size = "1KiB"
max_size = "1MiB"
statuses = ["success"]

[symbols]
mode = "auto"

